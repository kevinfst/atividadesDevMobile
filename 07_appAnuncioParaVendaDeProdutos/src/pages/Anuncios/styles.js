import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  titulo:{
    fontSize: 24,
    color: 'red',
    textAlign: 'center',
    marginTop: 40
  },

  tela:{
    marginTop: 40
  },
})

export {styles}